SELECT customers.id AS cust_id,
       orders.total AS order_total,
              COUNT(*) OVER (PARTITION BY customers.id) AS num_customer_orders
              FROM customers
              LEFT JOIN orders
                ON orders.customer_id = customers.id
                WHERE orders.total < 50.00;
